/**
 * Barrel export for the layers module.
 *
 * Consumers import from `./layers` and get the same public API.
 */
export { useDeckLayers } from './useDeckLayers';
export { POLYGON_PAINT, POINT_PAINT } from './mapStyles';
